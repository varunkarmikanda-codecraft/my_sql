STEP - 1
    let values_placeholder = '';
    for(let i = 0; i < keys.length; i++) {
      values_placeholder += "?";
      if(i < keys.length - 1) {
        values_placeholder += ', ';
      }
    }

STEP - 2
    const values_placeholder = "?".repeat(keys.length).split('').join(', ');
    
STEP - 3
    const values_placeholder = new Array(keys.length).fill('?').join(', ');
    
STEP - 4
    const values_placeholder = keys.map(_ => '?').join(', ');